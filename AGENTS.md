# Keel 工作约定

Keel 的目的是加快人与 AI 的协作效率：减少反复解释、等待和返工，让人更容易理解改动、判断结果、推进工作。让人跟得上 AI 是其中一部分。开发 Keel 本身也要做到这一点。

## 先让人看懂

- 用自然、好懂的中文写回复、文档和界面。先说结果，再解释原因。
- 正文要信息完整、说人话。保留理解问题和做决定所需的背景、原因、依据、改动、验证结果、限制和未解决的问题；不要为了简短删掉重要信息。
- 简化表达，不简化事实。技术细节、文件位置或操作步骤有助于理解、核查或继续工作时，应当写清楚；用分段和例子帮助阅读，避免重复和空话。
- 每次完整回复最后必须用 **总结** 作为醒目标记，下面列 2–4 条简短要点，每条只说一件事。摘要帮助快速抓住重点，不代替正文中的必要信息；只有内容本身很简单且信息不会遗漏时，才直接用总结作答。
- 少用术语；必须用时，解释它是什么意思。用具体例子说明复杂问题。
- 让用户能看懂现在做到哪、还差什么，不要让用户替你整理进度。

## 开始前

- 先读当前任务，再从 `docs/README.md` 找相关资料，按需阅读。查看 Git 状态及已有改动，不覆盖别人的工作。
- 一个 Issue 对应一个可单独验收的结果，不按文件或操作步骤拆分。AI 按资料索引中的“Issues 工作流”查重、整理和维护任务，不把这些工作交回用户。
- 复杂 Issue 默认关联一份 `plans/<任务名>.md`，写清目标、步骤和检查方法；简单任务无需计划。Issue 管整体状态，计划可勾选实施步骤，重要进展同步到 Issue，不维护第二套任务状态。

## 做事时

- 自主完成当前任务，不等用户催。只做目标需要的改动，发现其他问题先记下来。
- 一般实现细节自己判断；要改变产品方向或明显扩大范围时，再找用户确认。需要用户决定时，说清要决定什么、推荐哪个方案、主要取舍是什么。
- 只有产品或架构变化才更新相关设计文档；用户明确要求改文档时照办。日常进度放在当前任务记录里，不到处重复写。
- 新增、移动或删除文档时，同步更新 `docs/README.md` 的索引；具体资料的路径和用途统一放在那里。
- 工作约定只保留长期有用的内容；反复出现的问题再补规则，过时或重复的规则及时删。
- `workspace/` 是独立的工作区，修改前读它的 AGENTS.md，里面项目要单独检查它的 Git 状态。

## 完成前

- 运行相关测试。修 bug 要先确认问题能出现，再用同样的方法检查是否修好；不碰生产数据。
- 检查 diff（本次改动），包括新文件，确认没有无关修改。只改文档时检查内容和格式即可。
- 测试失败或没能运行就如实说明，不能写成通过。
- 更新已有任务记录或执行计划；没有记录时在当前会话说明状态。写清完成了什么、还差什么、新发现的问题和下一步，不擅自开始下一项任务。
- 完成任务后的总结先说结果和对用户的影响，再说验证情况、重要限制或需要用户决定的事；没有的项目不凑数。详细依据放在正文，影响结论的问题不能从摘要中省略。
